# SSL cert fetch credentials for news.wfc.welfarecall.com
# Copy to /etc/cyber-ai-news/cert-fetch.env and fill in.
# chmod 600 /etc/cyber-ai-news/cert-fetch.env — must be root-readable only.
CERT_FETCH_USER=
CERT_FETCH_PASSWORD=
