feedparser>=6.0.11
requests>=2.31.0
PyYAML>=6.0.1
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# The optional LLM fallback classifier needs an extra package — see
# requirements-llm.txt (kept separate as it's a large, slow install).
